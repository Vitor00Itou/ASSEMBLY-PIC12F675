;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*              MODIFICAÇÕES PARA USO COM 12F675                   *
;*                FEITAS PELO PROF. MARDSON                        *
;*                     MARÇO DE 2024                               *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                       NOME DO PROJETO                           *
;*                           CLIENTE                               *
;*         DESENVOLVIDO PELA MOSAICO ENGENHARIA E CONSULTORIA      *
;*   VERSÃO: 1.0                           DATA: 17/06/03          *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                     DESCRIÇÃO DO ARQUIVO                        *
;*-----------------------------------------------------------------*
;*   MODELO PARA O PIC 12F675                                      *
;*                                                                 *
;*                                                                 *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                     ARQUIVOS DE DEFINIÇÕES                      *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
#INCLUDE <p12f675.inc>	;ARQUIVO PADRÃO MICROCHIP PARA 12F675

	__CONFIG _BODEN_OFF & _CP_OFF & _PWRTE_ON & _WDT_OFF & _MCLRE_ON & _INTRC_OSC_NOCLKOUT

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                    PAGINAÇÃO DE MEMÓRIA                         *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;DEFINIÇÃO DE COMANDOS DE USUÁRIO PARA ALTERAÇÃO DA PÁGINA DE MEMÓRIA
#DEFINE	BANK0	BCF STATUS,RP0	;SETA BANK 0 DE MEMÓRIA
#DEFINE	BANK1	BSF STATUS,RP0	;SETA BANK 1 DE MAMÓRIA

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                         VARIÁVEIS                               *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; DEFINIÇÃO DOS NOMES E ENDEREÇOS DE TODAS AS VARIÁVEIS UTILIZADAS 
; PELO SISTEMA

	CBLOCK	0x20	;ENDEREÇO INICIAL DA MEMÓRIA DE
					;USUÁRIO
		W_TEMP		;REGISTRADORES TEMPORÁRIOS PARA USO
		STATUS_TEMP	;JUNTO ÀS INTERRUPÇÕES
		MAIOR
		MENOR

		;NOVAS VARIÁVEIS

	ENDC			;FIM DO BLOCO DE MEMÓRIA
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                        FLAGS INTERNOS                           *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; DEFINIÇÃO DE TODOS OS FLAGS UTILIZADOS PELO SISTEMA

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                         CONSTANTES                              *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; DEFINIÇÃO DE TODAS AS CONSTANTES UTILIZADAS PELO SISTEMA
; INICIALIZAÇÃO DA EEPROM, DE ACORDO COM A DESCRIÇÃO NO ARQUIVO "Def_Rega_Formigas.inc"

;A PARTIR DO ENDEREÇO ZERO DA EEPROM, DADOS EM ORDEM ALEATÓRIA
	ORG 0x2100
	DE	0X89,0X1E,0X39,0XFE,0XC2,0X01,0XAB,0X33,0X63,0XD3,0X95,0X7B,0X38,0XD6,0X1E,0X48
	DE	0XDB,0XD8,0X86,0XFB,0XA5,0XFB,0X0C,0XBE,0X68,0X9B,0XD9,0X10,0XD8,0XEC,0X90,0X91
	DE	0XAA,0XBB,0XCC,0XDD,0XEE,0XF1,0XC9,0X77

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                           ENTRADAS                              *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; DEFINIÇÃO DE TODOS OS PINOS QUE SERÃO UTILIZADOS COMO ENTRADA
; RECOMENDAMOS TAMBÉM COMENTAR O SIGNIFICADO DE SEUS ESTADOS (0 E 1)

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                           SAÍDAS                                *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; DEFINIÇÃO DE TODOS OS PINOS QUE SERÃO UTILIZADOS COMO SAÍDA
; RECOMENDAMOS TAMBÉM COMENTAR O SIGNIFICADO DE SEUS ESTADOS (0 E 1)

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                       VETOR DE RESET                            *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

	ORG	0x00			;ENDEREÇO INICIAL DE PROCESSAMENTO
	GOTO	INICIO
	
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                    INÍCIO DA INTERRUPÇÃO                        *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; ENDEREÇO DE DESVIO DAS INTERRUPÇÕES. A PRIMEIRA TAREFA É SALVAR OS
; VALORES DE "W" E "STATUS" PARA RECUPERAÇÃO FUTURA

	ORG	0x04			;ENDEREÇO INICIAL DA INTERRUPÇÃO
	MOVWF	W_TEMP		;COPIA W PARA W_TEMP
	SWAPF	STATUS,W
	MOVWF	STATUS_TEMP	;COPIA STATUS PARA STATUS_TEMP

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                    ROTINA DE INTERRUPÇÃO                        *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; AQUI SERÃO ESCRITAS AS ROTINAS DE RECONHECIMENTO E TRATAMENTO DAS
; INTERRUPÇÕES

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                 ROTINA DE SAÍDA DA INTERRUPÇÃO                  *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; OS VALORES DE "W" E "STATUS" DEVEM SER RECUPERADOS ANTES DE 
; RETORNAR DA INTERRUPÇÃO

SAI_INT
	SWAPF	STATUS_TEMP,W
	MOVWF	STATUS		;MOVE STATUS_TEMP PARA STATUS
	SWAPF	W_TEMP,F
	SWAPF	W_TEMP,W	;MOVE W_TEMP PARA W
	RETFIE

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*	            	 ROTINAS E SUBROTINAS                      *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
; CADA ROTINA OU SUBROTINA DEVE POSSUIR A DESCRIÇÃO DE FUNCIONAMENTO
; E UM NOME COERENTE ÀS SUAS FUNÇÕES.
LE_EEPROM
;LER DADO DA EEPROM, CUJO ENDEREÇO É INDICADO EM W
;O DADO LIDO RETORNA EM W
	ANDLW	.127		;LIMITA ENDEREÇO MAX. 127
	BANK1				;ACESSO VIA BANK 1
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	BANK0				;POSICIONA PARA BANK 0
	RETURN

GRAVA_EEPROM
;ESCREVE UM DADO NA EEPROM, CUJO ENDEREÇO É INDICADO EM W
;O DADO A SER ESCRITO DEVE ESTAR ARMAZENADO NA VARIÁVEL DADO
	ANDLW	.127		;LIMITA ENDEREÇO MAX. 127
	BANK1				;ACESSO VIA BANK 1
	MOVWF	EEADR
	MOVF	MENOR,W
	MOVWF	EEDATA
	BSF		EECON1,WREN ;HABILITA ESCRITA
	BCF		INTCON,GIE	;DESLIGA INTERRUPÇÕES
	MOVLW	B'01010101'	;DESBLOQUEIA ESCRITA
	MOVWF	EECON2		;
	MOVLW	B'10101010'	;DESBLOQUEIA ESCRITA
	MOVWF	EECON2		;
	BSF		EECON1,WR ;INICIA A ESCRITA
AGUARDA
	BTFSC	EECON1,WR ;TERMINOU?
	GOTO	AGUARDA
	BSF		INTCON,GIE ;HABILITA INTERRUPÇÕES
	BANK0				;POSICIONA PARA BANK 0
	RETURN

SUBROTINA1

	;CORPO DA ROTINA

	RETURN

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                     INICIO DO PROGRAMA                          *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
	
INICIO
	BANK1				;ALTERA PARA O BANCO 1
	MOVLW	B'00000000' ;CONFIGURA TODAS AS PORTAS DO GPIO (PINOS)
	MOVWF	TRISIO		;COMO SAÍDAS
	CLRF	ANSEL 		;DEFINE PORTAS COMO Digital I/O
	MOVLW	B'00000100'
	MOVWF	OPTION_REG	;DEFINE OPÇÕES DE OPERAÇÃO
	MOVLW	B'00000000'
	MOVWF	INTCON		;DEFINE OPÇÕES DE INTERRUPÇÕES
	BANK0				;RETORNA PARA O BANCO
	MOVLW	B'00000111'
	MOVWF	CMCON		;DEFINE O MODO DE OPERAÇÃO DO COMPARADOR ANALÓGICO

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                     INICIALIZAÇÃO DAS VARIÁVEIS                 *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
	CLRF MAIOR ;LIMPA DADO
	MOVLW .255
	MOVWF MENOR
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                     ROTINA PRINCIPAL                            *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
MAIN

	;CORPO DA ROTINA PRINCIPAL
	BANK1				;ACESSO VIA BANK 1
	MOVLW .0
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR0
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR0
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .1
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR1
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR1
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	  
	MOVLW .2
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR2
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR2
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .3
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR3
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR3
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .4
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR4
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR4
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .5
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR5
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR5
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .6
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR6
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR6
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .7
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR7
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR7
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .8
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR8
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR8
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .9
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR9
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR9
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .10
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR10
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR10
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .11
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR11
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR11
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .12
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR12
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR12
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .13
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR13
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR13
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .14
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR14
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR14
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .15
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR15
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR15
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .16
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR16
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR16
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .17
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR17
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR17
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .18
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR18
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR18
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .19
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR19
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR19
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .20
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR20
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR20
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .21
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR21
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR21
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .22
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR22
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR22
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .23
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR23
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR23
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .24
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR24
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR24
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .25
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR25
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR25
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .26
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR26
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR26
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .27
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR27
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR27
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .28
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR28
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR28
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .29
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR29
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR29
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .30
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR30
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR30
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .31
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR31
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR31
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .32
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR32
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR32
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .33
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR33
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR33
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .34
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR34
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR34
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .35
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR35
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR35
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .36
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR36
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR36
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .37
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR37
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR37
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .38
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR38
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR38
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	MOVLW .39
	MOVWF	EEADR		;INDICA O END. DE LEITURA
	BSF		EECON1,RD	;INICIA O PROCESSO DE LEITURA
	MOVF	EEDATA,W	;COLOCA DADO LIDO EM W
	
	TESTA_MAIOR39
	    SUBWF MAIOR, W
	    MOVF  EEDATA,W
	    BTFSS STATUS, C  ;CHECA SE C=1 (OU SEJA, RESULTADO DA SUBTRAÇÃO É POSITIVO)
	    MOVWF MAIOR      ;SE FOR NEGATIVO, TEMOS UM NOVO MAIOR
	
	TESTA_MENOR39
	    SUBWF MENOR, W
	    MOVF  EEDATA,W
	    BTFSC STATUS, C  ;CHECA SE C=0 (OU SEJA, RESULTADO DA SUBTRAÇÃO É NEGATIVO)
	    MOVWF MENOR      ;SE FOR POSITIVO, TEMOS UM NOVO MENOR
	    
	ESCRITA_MAIOR1
	    	MOVLW	.40
		MOVWF	EEADR
		MOVF	MAIOR,W
		MOVWF	EEDATA
		BSF		EECON1,WREN ;HABILITA ESCRITA
		MOVLW	B'01010101'	;DESBLOQUEIA ESCRITA
		MOVWF	EECON2		;
		MOVLW	B'10101010'	;DESBLOQUEIA ESCRITA
		MOVWF	EECON2		;
		BSF		EECON1,WR ;INICIA A ESCRITA
	AGUARDA1
		BTFSC	EECON1,WR ;TERMINOU?
		GOTO	AGUARDA1
		
	ESCRITA_MENOR2
	    	MOVLW	.41
		MOVWF	EEADR
		MOVF	MENOR,W
		MOVWF	EEDATA
		BSF		EECON1,WREN ;HABILITA ESCRITA
		MOVLW	B'01010101'	;DESBLOQUEIA ESCRITA
		MOVWF	EECON2		;
		MOVLW	B'10101010'	;DESBLOQUEIA ESCRITA
		MOVWF	EECON2		;
		BSF		EECON1,WR ;INICIA A ESCRITA
	AGUARDA2
		BTFSC	EECON1,WR ;TERMINOU?
		GOTO	AGUARDA2
		
	BANK0 
	
	
FIM
	GOTO FIM

;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
;*                       FIM DO PROGRAMA                           *
;* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

	END
